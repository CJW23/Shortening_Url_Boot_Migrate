spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/boot_short_url?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: awdsd123

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  devtools:
    livereload:
      enabled: true

logging:
  level:
    org:hibernate:SQL: DEBUG

